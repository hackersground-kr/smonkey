- name: Set up JDK 1.8
  uses: actions/setup-java@v1
  with:
    java-version: 1.8

- name: Grant execute permission for gradlew
  run: chmod +x gradlew

- name: Prepare environment
  run: echo "BASE_URL=${{ secrets.SERVER_URL }}" >> $GITHUB_ENV

- name: Build with Gradle
  run: ./gradlew build
